name: GitHub Actions Demo
run-name: "${{ github.actor }} is testing out GitHub Actions 🚀"

on:
  push:

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Trigger: ${{ github.event_name }}"
      - run: echo "🐧 Runner: ${{ runner.os }}"
      - run: echo "🔎 Branch: ${{ github.ref }} | Repo: ${{ github.repository }}"

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: List files in the repository
        id: list_files
        run: |
          ls "${{ github.workspace }}"

      # ✅ This prints a concrete outcome (success/failure/cancelled)
      - name: Print step outcome
        run: echo "🍏 'List files' step outcome is: ${{ steps.list_files.outcome }}"
